namespace SpinSquad.Gacha
{
    /// <summary>Stat buff từ roll — chọn 1 trong 4 random đều (ROLL_MACHINE_SPEC §6.1).</summary>
    public enum BuffStatKind
    {
        HpPct = 0,
        DmgPct = 1,
        AtkSpeedPct = 2,
        CritChance = 3
    }
}
