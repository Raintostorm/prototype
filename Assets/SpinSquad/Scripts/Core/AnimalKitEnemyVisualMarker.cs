using UnityEngine;

namespace SpinSquad.Core
{
    /// <summary>Marks local animal-kit prototype enemies so generic tint/scale code does not flatten their art.</summary>
    public sealed class AnimalKitEnemyVisualMarker : MonoBehaviour
    {
    }
}
