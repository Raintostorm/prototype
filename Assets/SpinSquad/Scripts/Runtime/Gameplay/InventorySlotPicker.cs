using UnityEngine;

namespace SpinSquad.Core
{
    /// <summary>Collider ô inventory (0–15) — tap để đưa ally lên lưới nếu còn chỗ.</summary>
    public sealed class InventorySlotPicker : MonoBehaviour
    {
        public int SlotIndex;
    }
}
